// Enable monitoring and logging
timeline.enabled            = true
trace.enabled               = true
dag.enabled                 = true

// Define pipeline parameters
params {
    datasets_csv         = 'homo-sapiens-test.csv'
    embedding_key_ch     = 'X_umap'
    h5ad_ch              = '5774ef6a-4082-48f6-a692-674637671aa5.h5ad'
    label_key_ch         = 'subclass.l3'
    organism             = 'Homo sapiens'
    condition            = 'normal'
    tissue               = 'kidney'
    author               = 'name'
    publication_date     = 'year'
    publication          = 'publication_name'
    metric               = 'euclidean'
    outdir               = 'results'
    silhouette_score     = 'silhouette_score'
    save_annotation      = true
    save_cluster_summary = true
    save_scores          = true
    sort_by              = 'median'
    score_col            = 'silhouette_score'
}


// Enable docker
docker {
  enabled = true
  runOptions = '--pull always'
}

//        container = 'ghcr.io/nih-nlm/nsforest-nf/container/nsforest:latest'

process {
    withName: nsforest_process {
        container 'ralatsdio/nsforest:latest'

	maxForks = 15
        cpus = 16
        memory = '128 GB'
    }


}